<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:PortalProveedores.Mobile.ViewModels"
             xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
             xmlns:drawing="clr-namespace:CommunityToolkit.Maui.Views;assembly=CommunityToolkit.Maui"
             x:Class="PortalProveedores.Mobile.Views.RecepcionPage"
             x:DataType="vm:RecepcionViewModel"
             Title="Recepción de Mercadería">

    <Grid RowDefinitions="*,Auto,Auto">

        <zxing:CameraBarcodeReaderView Grid.Row="0"
                                     x:Name="barcodeReader"
                                     BarcodesDetected="BarcodesDetected" />

        <Border Grid.Row="1" Stroke="Gray" StrokeThickness="1" Margin="20" HeightRequest="200">
            <drawing:DrawingView
                x:Name="MyDrawingView"
                LineColor="Black"
                LineWidth="5"
                BackgroundColor="White" />
        </Border>

        <Button Grid.Row="2" 
                Text="Confirmar Recepción y Enviar Firma"
                Margin="20"
                IsEnabled="{Binding IsNotBusy}"
                Command="{Binding ConfirmarRecepcionCommand}"
                CommandParameter="{x:Reference MyDrawingView}" />
    </Grid>
</ContentPage>